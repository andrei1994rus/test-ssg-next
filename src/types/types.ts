export type errorData = Array<String>; /*string[]*/
export type errorModal = {
	message: string;
	code: string;
};
export type dataModal = {
	name: string;
};
export type Props = {
	params: {
		id: string;
	};
};
